name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Copy verification files and sitemap (fallback)
        run: |
          # Копируем файлы верификации (если они в корне)
          cp google160c624a30a8e77e.html dist/ 2>/dev/null || true
          cp yandex_3b062bfe6c173430.html dist/ 2>/dev/null || true
          # Если sitemap.xml вдруг не попал из client/public, скопируем из корня (но обычно он уже в dist)
          cp sitemap.xml dist/ 2>/dev/null || true
          cp robots.txt dist/ 2>/dev/null || true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4